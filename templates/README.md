# Templates README.md

This directory contains all the templateer templates created and available to the user. Each template should be in its own named folder. That template folder will contain the template files, as well as all testing, validation, examples, and output generation. The template files themselves will be copied to the appropriate location for external ingestion after all test/validation CI/CD workflows complete.
